requests>=2.32
beautifulsoup4>=4.12
lxml>=5.2
rapidfuzz>=3.9        # optional: fuzzy keyword fallback, safe to omit
python-dateutil>=2.9
python-dotenv>=1.0     # local .env secrets; no-op in GitHub Actions

# Optional, heavier NLP upgrade path -- not required by this package;
# only if you replace the curated-keyword-variant approach with lemmatization:
# classla>=2.1
